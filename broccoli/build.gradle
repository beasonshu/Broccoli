apply plugin: 'com.android.library'
apply plugin: 'maven-publish'

android {
    compileSdk 34

    defaultConfig {
        minSdkVersion 23
    }
    namespace 'me.samlss.broccoli'
    lint {
        abortOnError false
    }

    publishing{
        singleVariant("release"){

        }
    }

}

dependencies {
}
afterEvaluate{
    publishing{
        publications{
            release(MavenPublication){
                from components.release
                groupId='com.github.beasonshu'
                artifactId = 'broccoli'
                version = '1.0.0'
            }
        }
    }
}